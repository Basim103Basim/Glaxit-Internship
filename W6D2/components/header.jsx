import { useContext } from "react";
import { ThemeContext } from "../themeContext";
function Header(){
    return(
        <header>
            <h1>useContext</h1>
            <p>Learn everything about Javascript</p>
        </header>
    )
}
export default Header